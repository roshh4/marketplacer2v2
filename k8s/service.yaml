apiVersion: v1
kind: Service
metadata:
  name: marketplace-backend-service
  namespace: marketplace
  labels:
    app: marketplace-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: marketplace-backend
